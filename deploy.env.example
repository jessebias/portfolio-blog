# Copy this file to deploy.env (gitignored) and fill in your server details.
# deploy.sh sources these values.

# SSH target: <user>@<server-ip-or-dns>
HOST=ubuntu@YOUR_SERVER_IP

# Path to the SSH private key for the instance (a leading ~ is expanded)
KEY=~/.ssh/portfolio-blog-prod.pem
